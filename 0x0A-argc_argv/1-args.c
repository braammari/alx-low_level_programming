#include <stdio.h>
#include<stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * @argc: is integer count
 * @argv: is an array
 * Return: Always 0 (Success)
 */

int main(int argc, char __attribute__((unused)) argv[])
{
	printf("%s\n", argc);
	return (0);
}
